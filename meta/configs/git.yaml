- shell:
    - ['sudo apt install --no-install-recommends -y git', Install deps]

- link:
    ~/.gitconfig:
        path: apps/git/gitconfig

    ~/.config/git/ignore:
        path: apps/git/ignore
        force: true
        create: true
