- shell:
    - ['[ -z "$WSL_DISTRO_NAME" ] || sudo apt install -y wslu socat xdotool', install wslu]

- link:
    ~/.config/systemd/user/ssh-agent.service: apps/wsl/.config/systemd/user/ssh-agent.service
    ~/.config/systemd/user/x410-vsock.service: apps/wsl/.config/systemd/user/x410-vsock.service
    ~/.local/etc/sh.d/25-wsl-env.sh: apps/wsl/25-wsl-env.sh
    ~/.local/bin/win-gpg-agent-relay: apps/wsl/win-gpg-agent-relay
    ~/.local/bin/gatherwindows: apps/wsl/gatherwindows
